# Auto detect text files and perform LF normalization
* text=auto

# Binarios: sin normalización de fin de línea ni diff textual. Git ya los detecta
# por contenido, pero dejarlo explícito evita sorpresas con el banner y los iconos.
*.jpg  binary
*.jpeg binary
*.png  binary
*.ico  binary
